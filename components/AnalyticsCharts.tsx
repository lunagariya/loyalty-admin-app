'use client';import {useUi} from './Providers';import {MonthlyGrowth} from '@/lib/types';import {Bar,BarChart,CartesianGrid,ResponsiveContainer,Tooltip,XAxis,YAxis} from 'recharts';export function AnalyticsCharts({data}:{data:MonthlyGrowth[]}){const{dark}=useUi();const points=data.map(x=>({month:new Date(x._id.year,x._id.month-1).toLocaleString(undefined,{month:'short'}),members:x.members}));return <div className="chart-wrap"><ResponsiveContainer width="100%" height="100%"><BarChart data={points}><CartesianGrid strokeDasharray="3 3" stroke={dark?'#454850':'#ddd'}/><XAxis dataKey="month" stroke={dark?'#ddd':'#555'}/><YAxis allowDecimals={false} stroke={dark?'#ddd':'#555'}/><Tooltip contentStyle={{background:dark?'#1d2026':'white',borderColor:dark?'#555':'#ddd'}}/><Bar dataKey="members" name="New members" fill={dark?'#7aa7ff':'#005bd3'} radius={[5,5,0,0]}/></BarChart></ResponsiveContainer></div>}

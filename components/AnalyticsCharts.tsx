'use client';import {MonthlyGrowth} from '@/lib/types';import {Bar,BarChart,CartesianGrid,ResponsiveContainer,Tooltip,XAxis,YAxis} from 'recharts';export function AnalyticsCharts({data}:{data:MonthlyGrowth[]}){const points=data.map(x=>({month:new Date(x._id.year,x._id.month-1).toLocaleString(undefined,{month:'short'}),members:x.members}));return <div className="chart-wrap"><ResponsiveContainer width="100%" height="100%"><BarChart data={points}><CartesianGrid strokeDasharray="3 3" stroke="#ddd"/><XAxis dataKey="month" stroke="#555"/><YAxis allowDecimals={false} stroke="#555"/><Tooltip contentStyle={{background:'white',borderColor:'#ddd'}}/><Bar dataKey="members" name="New members" fill="#005bd3" radius={[5,5,0,0]}/></BarChart></ResponsiveContainer></div>}

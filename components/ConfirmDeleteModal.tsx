'use client';import {Modal,Text} from '@shopify/polaris';export function ConfirmDeleteModal({open,itemName,onClose,onConfirm,loading}:{open:boolean;itemName:string;onClose:()=>void;onConfirm:()=>void;loading?:boolean}){return <Modal open={open} onClose={onClose} title={`Delete ${itemName}`} primaryAction={{content:'Delete',destructive:true,onAction:onConfirm,loading}} secondaryActions={[{content:'Cancel',onAction:onClose}]}><Modal.Section><Text as="p">Are you sure you want to delete this {itemName}? This cannot be undone.</Text></Modal.Section></Modal>}

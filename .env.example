# Public Shopify app Client ID (not the client secret)
NEXT_PUBLIC_SHOPIFY_API_KEY=replace_with_shopify_client_id
# Public URL of loyalty-backend; omit a trailing slash
NEXT_PUBLIC_BACKEND_URL=http://localhost:3000
# Optional, comma-separated hostnames used only when developing through a tunnel
ALLOWED_DEV_ORIGINS=
